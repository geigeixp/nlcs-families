@import "../../app.wxss";

page {
  background: #f6f6f6;
  padding-bottom: 40rpx;
}

/* Fixed Header Styles */
.fixed-header {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 100;
  background: #fff;
  box-shadow: 0 2rpx 10rpx rgba(0,0,0,0.05);
}

.header-spacer {
  height: 120rpx; /* Default height for unapproved state (only topbar) */
}

.header-spacer.header-spacer-approved {
  height: 360rpx; /* Increased height for approved state (topbar + filters) to prevent overlap */
}

/* Feed Topbar */
.feed-topbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20rpx 30rpx;
  background: #fff;
}

.feed-title {
  font-size: 36rpx;
  font-weight: bold;
  color: #333;
}

.feed-post-btn {
  background: #1296db;
  color: #fff;
  font-size: 26rpx;
  padding: 0 30rpx;
  height: 56rpx;
  line-height: 56rpx;
  border-radius: 28rpx;
  margin: 0;
}

/* Filters Area */
.feed-filters {
  background: #fff;
  padding-bottom: 10rpx;
}

.search-bar {
  display: flex;
  align-items: center;
  background: #f5f5f5;
  margin: 0 30rpx 20rpx;
  padding: 10rpx 20rpx;
  border-radius: 30rpx;
  height: 64rpx;
  box-sizing: border-box;
}

.search-input {
  flex: 1;
  font-size: 26rpx;
  margin-left: 10rpx;
}

.category-scroll {
  white-space: nowrap;
  margin-bottom: 20rpx;
  padding-left: 30rpx;
}

.category-row {
  display: flex;
  padding-right: 30rpx;
}

.pill {
  padding: 8rpx 24rpx;
  background: #f5f5f5;
  color: #666;
  font-size: 26rpx;
  border-radius: 28rpx;
  margin-right: 16rpx;
  transition: all 0.2s;
}

.pill.active {
  background: #e6f7ff;
  color: #1296db;
  font-weight: 500;
}

.filter-row {
  display: flex;
  padding-left: 30rpx;
  padding-right: 30rpx;
  gap: 16rpx;
}

.pill.small {
  margin-right: 0;
  padding: 6rpx 20rpx;
  font-size: 24rpx;
}

/* 瀑布流容器 */
.feed-container {
  padding: 0 20rpx;
}

/* 瀑布流网格布局 - 双列卡片 */
.cards-grid {
  display: flex;
  justify-content: space-between;
  padding: 0 10rpx;
}

.card-column {
  width: 48%;
  display: flex;
  flex-direction: column;
}

/* Card Styles - 优化为瀑布流卡片 */
.card {
  background: #fff;
  margin-bottom: 20rpx;
  border-radius: 16rpx;
  overflow: hidden;
  box-shadow: 0 4rpx 12rpx rgba(0,0,0,0.06);
  transition: transform 0.2s, box-shadow 0.2s;
}

/* 卡片悬停效果（虽然小程序不支持hover，但保留结构） */
.card-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  padding: 20rpx 20rpx 0;
  margin-bottom: 16rpx;
}

.header-left {
  display: flex;
  align-items: center;
  flex: 1;
}

.avatar {
  width: 60rpx;
  height: 60rpx;
  border-radius: 50%;
  margin-right: 16rpx;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  border: 2rpx solid #fff;
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.1);
}

.user-info {
  display: flex;
  flex-direction: column;
  justify-content: center;
}

.username {
  font-size: 26rpx;
  font-weight: 600;
  color: #333;
  line-height: 1.2;
}

.time {
  font-size: 22rpx;
  color: #999;
  margin-top: 6rpx;
}

.header-right {
  display: flex;
  gap: 20rpx;
}

.post-edit, .post-delete {
  font-size: 24rpx;
  color: #576b95;
  padding: 10rpx 0;
}

.card-content {
  padding: 0 20rpx;
  margin-bottom: 16rpx;
}

.category-tag {
  display: inline-block;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  color: #fff;
  padding: 4rpx 16rpx;
  border-radius: 20rpx;
  font-size: 22rpx;
  margin-bottom: 12rpx;
  font-weight: 500;
}

.content-text {
  font-size: 28rpx;
  color: #333;
  line-height: 1.7;
  display: -webkit-box;
  overflow: hidden;
  text-overflow: ellipsis;
  -webkit-box-orient: vertical;
  word-break: break-word;
}

.link-text {
  color: #576b95;
  text-decoration: underline;
  margin: 0 4rpx;
}

.highlight-text {
  color: #e53935; /* Red highlight color */
  font-weight: bold;
}

/* Image Grid - 瀑布流优化 */
.card-images {
  margin-bottom: 0;
}

.image-grid {
  display: grid;
  gap: 8rpx;
}

.grid-1 {
  grid-template-columns: 1fr;
}
.grid-2 {
  grid-template-columns: 1fr 1fr;
}
.grid-3 {
  grid-template-columns: repeat(3, 1fr);
}

.post-image {
  border-radius: 0;
  width: 100%;
  height: auto; /* 自适应高度，保持图片比例 */
  min-height: 200rpx;
  object-fit: cover;
}

/* 单图在瀑布流中全宽显示 */
.grid-1 .post-image {
  width: 100%;
  height: auto;
  min-height: 300rpx;
  max-height: 600rpx;
}

.card-footer {
  display: flex;
  justify-content: space-around;
  align-items: center;
  padding: 20rpx;
  border-top: 1rpx solid #f5f5f5;
  background: #fafafa;
}

.action-item {
  display: flex;
  align-items: center;
  justify-content: center;
  flex: 1;
}

.action-text {
  font-size: 24rpx;
  color: #999;
  white-space: nowrap;
}

.action-text.liked {
  color: #ff4757;
  font-weight: 600;
}

.share-wrapper {
  margin-left: 24rpx;
}

.share-btn-inline {
  background: transparent;
  padding: 0;
  margin: 0;
  line-height: 1.2;
  font-size: 26rpx;
  color: #666;
  border: none;
}

.share-btn-inline::after {
  border: none;
}

.loading-tip {
  text-align: center;
  padding: 30rpx;
  color: #999;
  font-size: 24rpx;
}

.gated-tip {
  background: #fff3bf;
  color: #7a5d00;
  padding: 20rpx;
  border-radius: 12rpx;
  margin-bottom: 24rpx;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.gated-btn {
  background: #fff;
  color: #7a5d00;
  border: 1rpx solid #7a5d00;
  margin: 0;
  font-size: 24rpx;
}

/* Match Comments */
.match-comments {
  background: #f9f9f9;
  padding: 16rpx;
  border-radius: 8rpx;
  margin-top: 20rpx;
}

.match-comment-item {
  font-size: 26rpx;
  margin-bottom: 8rpx;
  color: #666;
}

.match-comment-item:last-child {
  margin-bottom: 0;
}

.match-label {
  color: #999;
}

.match-author {
  color: #576b95;
  font-weight: 500;
}

.match-content {
  color: #333;
}

/* Author Modal */
.modal-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.5);
  z-index: 999;
}

.author-modal {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 600rpx;
  background: #fff;
  border-radius: 20rpx;
  z-index: 1000;
  overflow: hidden;
}

.author-card {
  width: 100%;
}

.author-card-header {
  background: #e6f7ff;
  padding: 40rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.author-card-avatar {
  width: 120rpx;
  height: 120rpx;
  border-radius: 50%;
  margin-bottom: 20rpx;
  border: 4rpx solid #fff;
}

.author-card-main {
  text-align: center;
}

.author-card-name {
  font-size: 36rpx;
  font-weight: 600;
  color: #333;
  display: block;
  margin-bottom: 10rpx;
}

.author-card-class {
  display: inline-block;
  background: #1296db;
  color: #fff;
  font-size: 24rpx;
  padding: 4rpx 16rpx;
  border-radius: 20rpx;
}

.author-card-body {
  padding: 30rpx 40rpx;
  min-height: 200rpx;
}

.info-row {
  display: flex;
  margin-bottom: 20rpx;
  font-size: 28rpx;
}

.info-label {
  color: #999;
  width: 140rpx;
  flex-shrink: 0;
}

.info-value {
  color: #333;
  flex: 1;
}

.info-empty {
  text-align: center;
  color: #999;
  padding: 40rpx 0;
}

.loading-view, .error-view {
  text-align: center;
  color: #999;
  padding: 40rpx 0;
}

.author-card-footer {
  border-top: 1rpx solid #f0f0f0;
  padding: 24rpx 0;
  text-align: center;
  font-size: 30rpx;
  color: #576b95;
  font-weight: 500;
}
